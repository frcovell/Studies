#include <stdio.h>

int main (void)
{
    int x; // x variable

    x = 1; // 1 constant literal

    printf("the value of x is: %i\n", x);

    
    //intergal type 
    // 0000 : 0
    // 0001 : 1
    // 0010 : 2
    char c; // 1-byte
    int i; // normally 32-bit
    long int li;
    long long int lli;
    
    // floating point type
    float f; // real decimal number
    double d; 
    long double ld;

    // basic aritmetic operator:
    // +, -, *, /, %
    // ^ not exponant/ power
    
   

    return x;
}